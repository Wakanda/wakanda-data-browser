export enum ColumnKinds {
    Storage = 'storage',
    RelatedEntity = 'relatedEntity',
    RelatedEntities = 'relatedEntities',
    calculated = 'calculated',
}

export enum ColumnTypes {
    Boolean = 'bool',
    Long = 'long',
    Number = 'number',
    String = 'string',
    Date = 'date',
    Blob = 'blob',
    Image = 'image',
}